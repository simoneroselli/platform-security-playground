---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: redis-from-vote-frontend
  namespace: default # 💡 Corrected to target your current active namespace
spec:
  podSelector:
    matchLabels:
      app: redis # Targets your backend pod inside 'default'
  ingress:
  - from:
    - podSelector:
        matchLabels:
          app: vote-frontend # Only accepts traffic from your frontend pod inside 'default'
    ports:
    - protocol: TCP
      port: 6379 # Matches the port your backend is listening on